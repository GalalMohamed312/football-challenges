import 'package:flutter/material.dart';

class RondoChallengeView extends StatelessWidget {
  const RondoChallengeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          Text("RondoChallengeView"),
        ],
      ),
    );
  }
}
